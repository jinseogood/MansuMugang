selectFoodALL=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_IMG_SRC, MI.MENU_INFO, M.MENU_NAME, M.MENU_MAIN, M.MENU_SUB, M.PRICE, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) AND GM.MENU_CODE NOT IN(SELECT GM.MENU_CODE FROM TB_USER U INNER JOIN USER_AL UA ON(U.U_CODE = UA.U_CODE) JOIN ALLERGY A ON(A.AL_CODE = UA.AL_CODE) JOIN GRAD G ON(A.AL_CODE = G.AL_CODE) JOIN GRAD_MENU GM ON(GM.GRAD_CODE = G.GRAD_CODE) WHERE U.U_CODE = ?)
selectFoodGo=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_IMG_SRC, MI.MENU_INFO, M.MENU_NAME, M.MENU_MAIN, M.MENU_SUB, M.PRICE, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.GO = 1 AND GM.MENU_CODE NOT IN(SELECT GM.MENU_CODE FROM TB_USER U INNER JOIN USER_AL UA ON(U.U_CODE = UA.U_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = UA.AL_CODE) JOIN GRAD G ON(A.AL_CODE = G.AL_CODE) JOIN GRAD_MENU GM ON(GM.GRAD_CODE = G.GRAD_CODE)  WHERE U.U_CODE = ?) AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.GO = 0)
selectFoodDang=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_IMG_SRC, MI.MENU_INFO, M.MENU_NAME, M.MENU_MAIN, M.MENU_SUB, M.PRICE, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.DANG = 1 AND GM.MENU_CODE NOT IN(SELECT GM.MENU_CODE FROM TB_USER U INNER JOIN USER_AL UA ON(U.U_CODE = UA.U_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = UA.AL_CODE) JOIN GRAD G ON(A.AL_CODE = G.AL_CODE) JOIN GRAD_MENU GM ON(GM.GRAD_CODE = G.GRAD_CODE)  WHERE U.U_CODE = ?) AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.DANG = 0)
selectFoodHead=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_IMG_SRC, MI.MENU_INFO, M.MENU_NAME, M.MENU_MAIN, M.MENU_SUB, M.PRICE, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.HEAD = 1 AND GM.MENU_CODE NOT IN(SELECT GM.MENU_CODE FROM TB_USER U INNER JOIN USER_AL UA ON(U.U_CODE = UA.U_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = UA.AL_CODE) JOIN GRAD G ON(A.AL_CODE = G.AL_CODE) JOIN GRAD_MENU GM ON(GM.GRAD_CODE = G.GRAD_CODE)  WHERE U.U_CODE = ?) AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.HEAD = 0)
selectFoodGoDang=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_IMG_SRC, MI.MENU_INFO, M.MENU_NAME, M.MENU_MAIN, M.MENU_SUB, M.PRICE, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.GO = 1 AND G.DANG = 1 AND GM.MENU_CODE NOT IN(SELECT GM.MENU_CODE FROM TB_USER U INNER JOIN USER_AL UA ON(U.U_CODE = UA.U_CODE) JOIN ALLERGY A ON(A.AL_CODE = UA.AL_CODE) JOIN GRAD G ON(A.AL_CODE = G.AL_CODE) JOIN GRAD_MENU GM ON(GM.GRAD_CODE = G.GRAD_CODE) WHERE U.U_CODE = ?) AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.GO = 0 OR G.DANG = 0)
selectFoodGoHead=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_IMG_SRC, MI.MENU_INFO, M.MENU_NAME, M.MENU_MAIN, M.MENU_SUB, M.PRICE, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.GO = 1 AND G.HEAD = 1 AND GM.MENU_CODE NOT IN(SELECT GM.MENU_CODE FROM TB_USER U INNER JOIN USER_AL UA ON(U.U_CODE = UA.U_CODE) JOIN ALLERGY A ON(A.AL_CODE = UA.AL_CODE) JOIN GRAD G ON(A.AL_CODE = G.AL_CODE) JOIN GRAD_MENU GM ON(GM.GRAD_CODE = G.GRAD_CODE) WHERE U.U_CODE = ?) AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.GO = 0 OR G.HEAD = 0)
selectFoodDangHead=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_IMG_SRC, MI.MENU_INFO, M.MENU_NAME, M.MENU_MAIN, M.MENU_SUB, M.PRICE, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.DANG = 1 AND G.HEAD = 1 AND GM.MENU_CODE NOT IN(SELECT GM.MENU_CODE FROM TB_USER U INNER JOIN USER_AL UA ON(U.U_CODE = UA.U_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = UA.AL_CODE) JOIN GRAD G ON(A.AL_CODE = G.AL_CODE) JOIN GRAD_MENU GM ON(GM.GRAD_CODE = G.GRAD_CODE)  WHERE U.U_CODE = ?) AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.DANG = 0 OR G.HEAD = 0)
selectFoodGoDangHead=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_IMG_SRC, MI.MENU_INFO, M.MENU_NAME, M.MENU_MAIN, M.MENU_SUB, M.PRICE, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) LEFT JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.GO = 1 AND G.DANG = 1 AND G.HEAD = 1 AND GM.MENU_CODE NOT IN(SELECT GM.MENU_CODE FROM TB_USER U INNER JOIN USER_AL UA ON(U.U_CODE = UA.U_CODE) JOIN ALLERGY A ON(A.AL_CODE = UA.AL_CODE) JOIN GRAD G ON(A.AL_CODE = G.AL_CODE) JOIN GRAD_MENU GM ON(GM.GRAD_CODE = G.GRAD_CODE) WHERE U.U_CODE = ?) AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) JOIN ALLERGY A ON(A.AL_CODE = G.AL_CODE) WHERE G.GO = 0 OR G.DANG = 0 OR G.HEAD = 0)
insertMenuBuy=INSERT INTO BUY VALUES(SEQ_BUYNO.NEXTVAL, ?, ?, SYSDATE, DEFAULT, ?, ?, ?)
menuListG=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_INFO, M.MENU_NAME, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) WHERE G.GO = 1 AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) WHERE G.GO = 0) ORDER BY MENU_CODE ASC
menuListD=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_INFO, M.MENU_NAME, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) WHERE G.DANG = 1 AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) WHERE G.DANG = 0) ORDER BY MENU_CODE ASC
menuListH=SELECT DISTINCT MI.MENU_IMG_ENAME, MI.MENU_INFO, M.MENU_NAME, M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) WHERE G.HEAD = 1 AND M.MENU_CODE NOT IN(SELECT DISTINCT M.MENU_CODE FROM MENU M JOIN MENUINFO MI ON(M.MENU_CODE = MI.MENU_CODE) INNER JOIN GRAD_MENU GM ON(M.MENU_CODE = GM.MENU_CODE) JOIN GRAD G ON(G.GRAD_CODE = GM.GRAD_CODE) WHERE G.HEAD = 0) ORDER BY MENU_CODE ASC
insertLike=INSERT INTO TB_LIKE VALUES(SEQ_LIKENO.NEXTVAL, ?, ?)
deleteLike=DELETE FROM TB_LIKE WHERE U_CODE = ? AND MENU_CODE = ?
likeCheck=SELECT U_CODE, MENU_CODE FROM TB_LIKE WHERE U_CODE = ?

 

insertCart1=INSERT INTO BUY VALUES(SEQ_BUYNO.NEXTVAL, ?, ?, SYSDATE, 1, ?, ?+500, ?)
insertCart2=INSERT INTO BUY VALUES(SEQ_BUYNO.NEXTVAL, ?, ?, SYSDATE, 1, ?, ?, ?)
selectList=SELECT DISTINCT SUM(PRICE) AS PRICE2,DIET_NO, USER_MENU_NAME, BUY_DATE, U_CODE FROM BUY WHERE U_CODE=? AND STATUS = '1' GROUP BY (DIET_NO, USER_MENU_NAME, BUY_DATE, U_CODE) ORDER BY DIET_NO DESC 
